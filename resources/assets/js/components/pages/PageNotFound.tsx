import * as React from 'react';
import * as ReactDOM from 'react-dom';

export default class PageNotFound extends React.Component {
    render() {
        return (
            <h1>Hmmm, it looks like something is wrong.</h1>
        );
    }
}
