type Pressure {
    id: ID!
    pressure: Int
    date: String
}

type Temperature {
    id: ID!
    temperature: Float
    date: String
}

type Wind {
    id: ID!
    speed: Float
    direction: String
    date: String
}
